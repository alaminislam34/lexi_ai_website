# name: CI/CD Pipeline

# on:
#   push:
#     branches:
#       - main

# env:
#   DOCKER_REPO: fahad1000mir/trekperk-frontend
#   WEBSITE_TAG_LATEST: frontend-latest

# jobs:
#   dockerhub:
#     name: Build, Tag and Push on Docker Hub
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Run tests
#         run: |
#           echo "Skipping tests"

#       - name: Create .env file
#         run: |
#           echo "NEXT_PUBLIC_API_BASE_URL=${{ secrets.NEXT_PUBLIC_API_BASE_URL }}" >> .env
#           echo "NEXT_PUBLIC_AI_BASE_URL=${{ secrets.NEXT_PUBLIC_AI_BASE_URL }}" >> .env
#           echo "NEXT_PUBLIC_WS_URL=${{ secrets.NEXT_PUBLIC_WS_URL }}" >> .env

#       - name: Install docker-compose
#         run: |
#           sudo curl -L "https://github.com/docker/compose/releases/download/1.29.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
#           sudo chmod +x /usr/local/bin/docker-compose

#       - name: Log in to Docker Hub
#         env:
#           DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
#           DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
#         run: |
#           echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin

#       - name: Build and tag Frontend image
#         env:
#           NEXT_PUBLIC_API_BASE_URL: ${{ secrets.NEXT_PUBLIC_API_BASE_URL }}
#           NEXT_PUBLIC_AI_BASE_URL: ${{ secrets.NEXT_PUBLIC_AI_BASE_URL }}
#           NEXT_PUBLIC_WS_URL: ${{ secrets.NEXT_PUBLIC_WS_URL }}
#         run: |
#           docker-compose -f docker-compose.dev.yml build --no-cache
#           IMAGE_ID=$(docker images --format "table {{.Repository}}:{{.Tag}}\t{{.ID}}" | grep -v "REPOSITORY" | head -1 | awk '{print $2}')
#           docker tag $IMAGE_ID $DOCKER_REPO:$WEBSITE_TAG_LATEST

#       - name: Push Frontend image
#         run: docker push $DOCKER_REPO:$WEBSITE_TAG_LATEST

#   deploy:
#     name: Deploy to EC2
#     runs-on: ubuntu-latest
#     needs: dockerhub

#     steps:
#       - name: SSH to EC2 and deploy
#         uses: appleboy/ssh-action@v1.0.0
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ${{ secrets.EC2_USER }}
#           key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
#           script: |
#             set -e
#             cd /home/ubuntu/frontend
#             docker compose -f docker-compose.prod.yml down
#             docker compose -f docker-compose.prod.yml pull
#             docker compose -f docker-compose.prod.yml up -d --force-recreate --remove-orphans
#             sudo systemctl restart nginx